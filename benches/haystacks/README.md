# Benchmark corpora

This corpus was copied from the `memchr` crate's benchmark suite
(<https://github.com/BurntSushi/memchr>, `benchmarks/haystacks`), so results here
are comparable with the numbers that crate publishes. `sherlock/huge.txt` is the
classic memchr corpus used by `benches/memchr_n.rs`.
